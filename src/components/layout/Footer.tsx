/**
 * Footer — DIGMARK mark + tagline (UI_DESIGN_SPEC.md §C.18).
 */
export function Footer() {
  return (
    <footer className="mt-12 border-t border-divider py-6 text-center">
      <div className="text-[1.08rem] font-extrabold tracking-[0.05em] text-brand">DIGMARK</div>
      <div className="mt-2 text-[0.9rem] text-muted">Digital Marketing Command Center</div>
      <div className="mt-1 text-[0.9rem] text-muted">LPK Duta Persada</div>
    </footer>
  );
}